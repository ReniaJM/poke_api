import React from "react";

const Pokemon = (props) => {
    return (
        <>
          <div>
              <div style={{color:"red"}}>{props.name}</div>
          </div>

        </>
    );
};

export default Pokemon;


